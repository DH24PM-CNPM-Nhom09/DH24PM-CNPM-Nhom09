# URL gốc của Backend API (Node.js + Express).
# Khi Backend đã sẵn sàng, copy file này thành .env.local, điền URL thật,
# và đổi USE_MOCK = false trong src/lib/api.ts
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
